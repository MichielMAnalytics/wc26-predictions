# Copy to /home/boxd/.config/wc26-chat.env (chmod 600) — NEVER commit the real file.
# The chat agent reads ANTHROPIC_API_KEY from the environment via the unit's EnvironmentFile.
ANTHROPIC_API_KEY=sk-ant-...
